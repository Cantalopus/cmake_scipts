# Writes and configures CMakeList.txt, CMakePreset, main.cpp, utilities.ixx and vcpkg.json

## In Project root, run... 
```bash
cmake -P <path-to-script.cmake>
``` 

## Example... 
```bash
cmake -P ~/cmake_scripts/sfml-fmt.cmake 
```
